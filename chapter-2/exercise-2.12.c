// Exercises 2.12
// What, if anything, prints when each of the following statements is performed?
// If nothing prints, then answer "Nothing."
// Assume x = 2 and y = 3.

a)  printf( "%d", x );
    "2"

b)  printf( "%d", x + x );
    Nothing

c)  printf( "x=" );
    "2="

d)  printf( "x=%d", x );
    "2=2"

e)  printf("%d = %d", x + y, y + x );
    Nothing

f)  z = x + y;
    Nothing

g)  scanf( "%d%d", &x, &y );
    Nothing

h)  /* printf( "x + y = %d", x + y ); */
    Nothing

i)  printf("\n");
    Nothing
