// Exercises 2.9
// Write a single C statement or line that accomplishes each of the following:

// a)   Print the message "Enter two numbers."
        printf( "Enter two numbers.\n" );

// b)   Assign the product of variables b and c to variable a.
        a = b * c;

// c)   State that a program performs a sample payroll calculation (i.e., use text
//      that helps to document a program).
        /* Performs a sample payroll calculation */

// d)   Input three integer values from the keyboard and place these values in
//      integer variables a, b, and c.

        scanf( "%d%d%d", &a, &b, &c );
